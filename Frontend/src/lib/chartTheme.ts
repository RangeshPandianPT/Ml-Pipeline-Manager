// Shared chart theming utility
export const chartTooltipStyle = {
  background: "hsl(var(--card))",
  border: "1px solid hsl(var(--border))",
  borderRadius: "10px",
  color: "hsl(var(--foreground))",
  boxShadow: "0 8px 32px -8px rgba(0,0,0,0.15)",
  padding: "10px 14px",
  fontSize: "12px",
};

export const chartGridColor = "hsl(var(--border))";
export const chartTickStyle = { fill: "hsl(var(--muted-foreground))", fontSize: 12 };
