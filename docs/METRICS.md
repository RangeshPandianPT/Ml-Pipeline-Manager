# API Metrics

This project exposes real-time metrics using `prometheus-fastapi-instrumentator`.

## Accessing Metrics
The metrics endpoint is available at `/metrics`.

## What is Tracked?
- Request duration
- Request count by HTTP status code
- Current in-progress requests
